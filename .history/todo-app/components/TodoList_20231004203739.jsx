import React from 'react'

export const TodoList = ({todos, colorMode}) => {
  return (
    <>
        {
            todos.length > 0 && todos.map((todo) =>
            
            );
        };
    </>
  )
}
